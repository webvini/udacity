import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cardWrapper: {
    flex: 1,
    padding: 20,
  },
  input: {
    color: '#999',
    marginTop: 20,
    marginBottom: 20,
    padding: 10,
    borderWidth: 1,
    borderColor: '#ccc'
  }
})