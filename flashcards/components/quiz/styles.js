import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    textAlign: 'center'
  },
  answer: {
    fontSize: 24,
    textAlign: 'center',
    color: "#ccc"
  }
})