import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  step: {
    fontSize: 16,
    color: 'blue'
  },
  title: {
    fontSize: 24,
    textAlign: 'center'
  },
  answer: {
    fontSize: 24,
    textAlign: 'center',
    color: "#ccc"
  },
  done: {
    fontSize: 28,
    textAlign: 'center',
    color: "green"
  }
})